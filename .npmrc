# NPM Configuration for Vercel
# Prevents npm from running any postinstall scripts or looking for server files

# Skip any custom scripts (postinstall, etc)
ignore-scripts=false

# Use legacy peer deps for compatibility
legacy-peer-deps=true

# Strict version requirements
engine-strict=false

# Production install
production=false
