NODE_ENV=development
DATABASE_URL=postgresql://postgres:password@db.PROJECT.supabase.co:6543/postgres?sslmode=require
SESSION_SECRET=change-this-in-production

# Supabase (storage) — remote
# SUPABASE_URL=https://YOUR-PROJECT.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=YOUR_SERVICE_ROLE_KEY

# Supabase local (e.g. after: supabase start)
# Get URL and keys from: supabase status
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_SERVICE_ROLE_KEY=YOUR_SERVICE_ROLE_KEY

SESSION_USE_MEMORY=true

# Smart search: Gemini API for understanding user text and matching to categories (optional)
# Get key from: https://aistudio.google.com/app/apikey
# GEMINI_API_KEY=your_gemini_api_key

# Google OAuth (general users: email/password or Google sign-in only; no Apple)
# Create credentials at https://console.cloud.google.com/apis/credentials
# Add authorized redirect URI: https://your-domain.com/api/auth/google/callback (or http://localhost:5000/api/auth/google/callback for dev)
GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret

# Base URL for OAuth redirect_uri and post-login redirect (required for Google sign-in)
# Dev: http://localhost:5000  Prod: https://your-domain.com
BASE_URL=http://localhost:5000
