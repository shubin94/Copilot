NODE_ENV=development
# Required: Postgres connection (Supabase or local)
DATABASE_URL=postgresql://postgres:password@db.PROJECT.supabase.co:6543/postgres?sslmode=require
# Required: session + app secrets encryption (use SESSION_SECRET or set APP_SECRETS_ENCRYPTION_KEY)
SESSION_SECRET=change-this-in-production
# Optional: encrypt App Secrets in DB (32+ chars). If unset, SESSION_SECRET is used.
# APP_SECRETS_ENCRYPTION_KEY=
SUPABASE_URL=https://YOUR-PROJECT.supabase.co
SUPABASE_SERVICE_ROLE_KEY=YOUR_SERVICE_ROLE_KEY
SESSION_USE_MEMORY=true
# Before first run: npm run db:ensure-app-secrets (creates app_secrets table)

