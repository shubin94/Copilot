# Required: Database connection (only credential that must stay in env)
# Secrets like Google OAuth, Supabase, etc. are stored in app_secrets table
DATABASE_URL=postgresql://postgres:password@db.PROJECT.supabase.co:6543/postgres?sslmode=require

# Optional: For local dev, use memory session (no DB table needed)
SESSION_USE_MEMORY=true

# Optional: Dev fallbacks - in production, use Admin > App Secrets (Auth) or seed script
# Run: npx tsx scripts/seed-app-secrets.ts  (copies env to DB once)
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# SESSION_SECRET=
# BASE_URL=http://localhost:5000
# SUPABASE_URL=
# SUPABASE_SERVICE_ROLE_KEY=
